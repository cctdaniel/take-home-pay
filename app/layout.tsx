import { TAX_YEAR } from "@/lib/constants/tax-year";
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Base metadata - country-specific metadata is set in each [country]/page.tsx
export const metadata: Metadata = {
  title: `Take Home Pay Calculator ${TAX_YEAR}`,
  description: `Free ${TAX_YEAR} take home pay calculator. Calculate your salary after taxes for US, Singapore, South Korea, and Netherlands.`,
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <head>
        <meta name="author" content="Take Home Pay Calculator" />
        <link rel="icon" href="/favicon.ico" sizes="any" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
